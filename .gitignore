# 项目开发工作目录
# 对外发布组件：SKILL.md / scripts/ / references/ / doc/DESIGN.md（结果文档）

# ── 开发过程产物（不推送 GitHub）──
_proc-use/
.serena/

# ── 开发工具链（仅本地使用）──
.githooks/
.markdownlint.json
commitlint.config.mjs

# ── GitHub Actions / CI（启用 ci.yml 子文件跟踪）──
.github/*
!.github/workflows/
!.github/workflows/ci.yml

# ── doc/ 只保留 DESIGN.md ──
doc/*
!doc/DESIGN.md

# ── 运行时瞬态状态 ──
.phase-execution/

# ── 系统文件 ──
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# ── 临时文件 ──
*.tmp
*.log
*~
